# AutoForge 环境变量示例
#
# 复制本文件为 .env 后按需填写。.env 已被 .gitignore 忽略，不会提交。
# 注意：本文件仅用于「开发/构建」相关的环境变量。
# LLM api_key、MCP env、web_search api_key 等敏感密钥不在此处配置——
# 它们经应用内 Settings 页面录入，并由 core/secrets.rs 信封加密落库，
# 禁止以明文写入 .env 或前端代码（见 .autoforge/specs/api.md）。

# —— 开发服务器 ——————————————————————————————————————————————

# 远程开发主机（可选）。设置后 Vite dev server 监听该地址，HMR 走 ws://<host>:<port+1>。
# 常用于在另一台机器/移动端访问 dev server。留空则仅本机（host=false）。
# 来源：vite.config.ts、Tauri 官方移动端/远程开发约定。
# TAURI_DEV_HOST=192.168.1.100

# 开发端口（可选，优先级高于 AUTOFORGE_DEV_PORT）。
# 同时作用于 Vite dev server 与 Tauri devUrl（scripts/tauri-dev.mjs 注入同一端口）。
# 默认 1420。
# PORT=1420

# 开发端口的备用变量名（可选）。仅在未设置 PORT 时生效，默认 1420。
# AUTOFORGE_DEV_PORT=1420

# —— 前端构建期变量（可选） ——————————————————————————————————

# Vite 仅暴露以 VITE_ 前缀的变量给前端代码（import.meta.env.VITE_*）。
# 按需自定义，例如：
# VITE_APP_TITLE=AutoForge

# —— Tauri 构建期变量（可选，通常由 tauri CLI 自动注入，无需手填） ——
#
# 以下变量由 tauri build/dev 流程自动设置，列出仅供参考；
# vite.config.ts 据此调整构建目标与 sourcemap，手动覆盖需谨慎。
# TAURI_ENV_PLATFORM=linux       # 目标平台（windows 时构建 target 切到 chrome105）
# TAURI_ENV_DEBUG=               # 非空则关闭 minify 并产出 sourcemap
